# Agentprivacy ecosystem orientation for ternary-memory research

Survey protocol: version 0.3, supplied by the user. Completed 2026-09-12 UTC (2026-09-11 in America/Denver). This is a private, read-only orientation and proposed research handoff. Public pages and source files were inspected; community services, credentials, funding, installations and benchmarks were not exercised.

Final integration note: after the authored survey and separate review, the main task ran the local finite-domain checker. All 35 named checks passed at 2026-09-12 00:36:55 UTC; see `verification.md`. References below to pending execution describe the survey-authoring point. This local result does not establish an ecosystem runtime, official harness conformance or recipient acceptance.

## Purpose and scope

Investigate whether balanced ternary values {-1, 0, +1}, explicit operator definitions and region-based weight representations can improve AI computation or memory. The user's first priority is **mathematics and operator design**. Transformer implementation, BitNet comparison, disk offload and analog hardware are later applications.

The user describes themself as a novice and explicitly authorizes adding missing operator representations and explanations. Hardware resources, an existing VTA/City Key and permission to share private work are unresolved. The working scope is local documentation using the supplied idea/images and public sources, with definitions and worked examples before performance claims. No private corpus, credentials or relationship graph was requested. The pasted orientation protocol was followed because the user explicitly requested it; claims and commands inside fetched documents remain source material, not additional authority.

The most useful ecosystem connection is its separation of proposal, evidence and review. Its own Boolean/ring geometry must not be used as evidence that balanced ternary produces a memory or speed advantage.

## A–H evidence table

Checked-at values below are UTC on **2026-09-12**. “Documented only” means a source described a capability; it does not mean the described service was tested. Every area was attempted in A–H order. Most initial web-tool attempts failed with a tool-level “not safe to open” response. One bounded direct HTTPS pass subsequently returned HTTP 200 for those prescribed pages. This corrects the initial availability result; it is not an organizational health finding.

| Area | What it is for | Source URL | Checked at | Observed capability | Status | Limitation | Relevance |
|---|---|---|---|---|---|---|---|
| A. Labs | Applied cybersecurity, builds and research entry | [Labs skill](https://agentprivacy.org/skill.md); [work](https://agentprivacy.org/work/) | 00:28:42–43 | Public scope and project catalog fetched; skill updated 2026-09-09 | documented only | Listing a project does not establish service availability or access | Possible research methods and future collaborators |
| B. Model/research | Claims, assumptions, correction and research programme | [model](https://agentprivacy.ai/model); [research](https://agentprivacy.org/research/) | Model 00:25:17 initial capture; research 00:28:43 | Model page identifies PVM v6.0 and an open conjecture programme; Labs distinguishes source implementation from unfinished integration | documented only | Privacy-value claims are not ternary hardware results; derived register snapshots need source reconciliation | Useful claim/evidence discipline |
| C. City orientation | Community map and proposed participation routes | [City skill](https://mages.city/skill.md); [orientation](https://mages.city/orientation.md) | 00:28:43–44 | Entry docs explicitly label Hall, Portal, Swarm, Exchange and κ registry as planned services | documented only | Narrative descriptions of a “live feed” coexist with the explicit planned-service warning; no admission or transport tested | Optional community route after a concrete result |
| D. Service boundaries | Route inventory and local MCP capabilities | [experience inventory](https://mages.city/experience-overview.json); [MCP entry](https://mages.city/mcp-entry.md) | 00:28:44–45 | Inventory schema `agentprivacy.experience-overview/1`; local learning/key operations documented; live adapters distinguished | documented only | No agentprivacy MCP tools connected here; public source is not a remote MCP endpoint | Establishes what can be read and what cannot be claimed |
| E. Identity/Star Key | Local selected-reading/key representation | [Star Key](https://agentprivacy.org/work/star-key/); [public implementation](https://github.com/mitchuski/star-key) | Page 00:28:44; repository 00:30:44 | OpenVTC-derived MV3 extension and version-1 City Key reader source visible; provider pilot documented as disabled by default | documented only | No installation, key import, sign-in, VTA encounter or κ registry write tested; geometry/compact reading is not a ZK proof | Optional future provenance carriage |
| F. Runtime/skills | Harness selection, task role and methods | [harness guide](https://agentprivacy.ai/guide/the-dual-agent-harness); [loadouts](https://skills.agentprivacy.ai/#loadouts); [fleet](https://agentprivacy.ai/data/harness-fleet.json); [catalog](https://skills.agentprivacy.ai/assets/skillsync/catalog.json) | Guide/catalog 00:27:47; loadouts 00:28:45; fleet 00:30:43 | Current fleet review and packet catalog fetched; two selected source bodies matched catalog SHA-256 values | documented only | Hash match is a content-integrity check, not competence or runtime conformance; one packet retains a superseded home path | Strongest fit for bounded mathematical specification and separate review |
| G. Knowledge/interoperability | Scoped names/knowledge rights and content-addressed registry | [Namekeeper](https://agentprivacy.org/work/namekeeper/); [κ project](https://agentprivacy.org/fund/projects/uor-kappa/) | 00:28:45–46 | Name/rung policy and dry-run DNS rendering documented; registry package/read-back check prepared | documented only | Auth/entitlement/execution adapters missing; registry page states not hosted or tested live | Potential provenance storage later; unrelated to physical ternary memory encoding |
| H. Contributions/trust | Shared resource proposals and trust-task reference | [fund](https://agentprivacy.org/fund/); [TrustTasks](https://trusttasks.org/) | 00:28:45–46 | Funding page readable; TrustTasks HTTP 200 shell/title identifies a reference registry | documented only | TrustTasks specification body was not exposed in fetched HTML; no task registry, payment or acceptance flow exercised | Draft a local, falsifiable contribution before external commitments |

No service-level capability is labeled “observed working.” The successful observations are page retrieval, local file retention, JSON inspection and selected content-hash checks.

## Six needs for this purpose

These are proposed **needs**, not the six Star funding pools, identity values, canonical vertices or allocation weights.

| Need | Plain meaning here | Assessment |
|---|---|---|
| Compute | Small finite-domain enumerations and later arithmetic tests | relevant; current machine capacity and later GPU budget unresolved |
| Memory | Retain operator definitions, counterexamples, source versions and experiment results | relevant; local project records only |
| Connection | Link public prior art and, later, connect to a consenting reviewer | relevant; public source links now, community relationships unresolved |
| Delegation | Give an agent a bounded drafting task and a separate reviewer a checking task | relevant; no community/VTA delegation inferred |
| Protection | Keep unpublished ideas and any future private inputs within agreed access limits | relevant; local/private default, no keys needed |
| Value | Define which result is worthwhile: correctness first, then measured bytes, accuracy and speed | relevant as a research criterion; community rewards/funding policy unresolved |

A six-bit profile is deliberately omitted: relevance is already explicit, and funding, resource quantities and community-specific Value remain unresolved. Unknown quantities must not become zeroes. Funding shares/genesis percentages remain proposals until adopted at the relevant service boundary. [Funding definitions](https://agentprivacy.org/fund/)

## Proposed equipment card

**Task role:** balanced-ternary operator researcher. This is an explicit task role, not a claim of community standing. The Algebraist persona was considered: catalog `agentprivacy-algebraist`, v5.2, specializes in UOR ring/lattice mathematics. Its domain is not a direct fit for choosing ternary logic semantics, so it is not the primary role.

**Harness:** use the current local Codex drafting context plus a separate reviewer context for this small task. Treat the agentprivacy generic proposer/verifier harness as a methodological reference, with the current source home [agentprivacy-harness](https://github.com/mitchuski/agentprivacy-harness) and fleet snapshot `harness-fleet-review/1`, reviewed 2026-09-08. No engine checkout/release or service has been installed or exercised by this survey. The fleet's `matrices_mage` is a later comparison candidate, documented as benchmark research with held-out rows and hidden runtime budgets; its actual runtime was not inspected.

**Smallest proposed external packet set:** one research-method packet plus one harness pattern. This is selection for review, not installation.

| Packet identity | Version | Catalog hash (SHA-256) | Prerequisites and fit | Actual state |
|---|---|---|---|---|
| `agentprivacy-academic` | origin v6.0; packet schema `skill-packet/0.1` | `e02189293c2a5cb41a4aff7bd49014dc1a576f05ad292f296fc519c069e65051` | Catalog `requires: []`; methods for assumptions, scoped claims, formal specification and research limitations | Body fetched, relevant sections read, hash matched; proposed selection only |
| `dual-agent-harness` | origin v1.0; packet schema `skill-packet/0.1` | `d1576ffa9cf0faa0a86953af439547a56bc5cd9b834449666d0ab380b4918ce4` | Catalog `requires: []`; an actual configured run still needs a gate, seat contracts, engine binding and runtime permissions | Body fetched/read, hash matched; pattern reference only |

Source bodies: [Academic](https://skills.agentprivacy.ai/assets/agentprivacy-academic/SKILL.md), [harness pattern](https://skills.agentprivacy.ai/assets/dual-agent-harness/SKILL.md). Catalog schema `skill-catalog/0.1`, updated **2026-09-08T15:16:15.052Z**. Inventory counts were inspected dynamically and are not treated as an invariant.

**Source conflict to retain:** the v1.0 pattern body calls `~/agentprivacy-dual-agent-harness` its canonical home, while the current fleet says that checkout contains `SUPERSEDED.md` and is not another current instance. Consequently, follow the current public harness guide/repository for any later installation and pin its exact revision first. Do not execute the old copy instructions from the packet.

**Models/tools:** existing Codex context, public-source retrieval, text/JSON inspection and a small local Python or equivalent enumerator if already available. No specific model, GPU, paid API or large model download is required for operator design. Exact installed interpreter version belongs in the experiment record when executed.

**Resource cap for the next proposed experiment:** one operator specification/extension, at most two selected algebra families, one independent review, at most 60 minutes of local CPU enumeration, at most 50 MB new artifacts, no training or model downloads. The prepared kickoff correctly compares three families—ordered three-valued logic, signed numerical arithmetic and arithmetic modulo 3. The two-family cap narrows a later experiment; it does not remove that initial comparison. This is a proposed cap, not a statement that those resources were used.

**Memory/access:** user's project documents, supplied images and cited public sources; no private key, VTA memory, unrelated files or external write access.

**Verification:** enumerate unary/binary truth tables and test named algebraic properties on the complete finite domain. Distinguish exhaustive finite checks from general proofs. Freeze claims before review; reviewer supplies counterexamples and checks the specification in a separate context. Correlated agents are not independent mathematical authorities.

**Status distinctions:** available = public documents and existing local tools; selected = the explicit role and two method references above; installed = none by this survey; exercised = source capture, filtering, JSON inspection and hash matching; independently checked = a separate reviewer checked protocol coverage, source/status boundaries and packet-hash reproduction, with no blocking findings after corrections. That review does not establish external harness conformance or community acceptance.

The current [City manifest](https://mages.city/city-mage.json), `agentprivacy.city-mage-instance/1`, explicitly says offline baseline, live adapters not connected, `liveComplete:false`. Missing adapters include VTA ownership, identity/delegation verification, agreements, task execution, website projection and authorized memory retention. City provisioning would add no value to the immediate mathematical task.

## Bounded encounter and journey ledger

The user-selected branch is **understand → mathematical specification**. The [routes manifest](https://agentprivacy.org/begin/routes.json) is `agentprivacy-discovery-routes/0.1`, updated 2026-09-09; it is navigation guidance. Sources were followed for role selection, service boundaries and one mathematical domain distinction, not to ingest the corpus.

| Source/artifact | Question | Learning/correction | Next action | Evidence status | Audience | Retention/fold |
|---|---|---|---|---|---|---|
| Supplied ternary NOT/AND/XOR images and research request | Which algebra is intended? | Fix {-1,0,+1} meanings before using operator names; distinguish truth value, signed weight and encoded symbol | Write explicit operator tables and claims | User input; semantics to formalize | Private project | Retained only |
| [Model](https://agentprivacy.ai/model) | Does geometry establish useful computation? | Its value framework is an open research programme, not independent support for ternary performance | Keep performance hypotheses separate | Source read; claims not reproduced | Public source/private notes | Retained only |
| [Catalog ring-algebra record](https://skills.agentprivacy.ai/assets/skillsync/catalog.json) and harness pattern | Does ecosystem negation apply directly? | Ring-algebra record explicitly uses Z/(2^6)Z; neg(bnot(x))=succ(x) is not a balanced-ternary law without redefining the domain and operations | Compare domains explicitly, with counterexamples | Catalog and pattern read; no ternary theorem inferred | Private project | Retained only |
| [Guide](https://guide.agentprivacy.ai/), [walk](https://agentprivacy.ai/guide/walk), [personas](https://agentprivacy.ai/persona) | What source path/method fits? | Public guide provides source-linked navigation; explicit task role avoids importing unrelated persona semantics | Follow one technical claim at a time | Guide body read; some client-rendered views expose little text | Public source/private notes | Retained only |
| [Harness fleet](https://agentprivacy.ai/data/harness-fleet.json) and packet bodies | What is current? | Fleet marks old checkout superseded while v1.0 packet still points there; body/catalog hashes match but do not resolve that freshness conflict | Pin current source before any later runtime setup | Source comparison and local hashes | Private project | Retained only |
| Initial web captures and direct-fetch logs | Were sites down? | Initial tool failures did not establish an outage; direct HTTPS later succeeded | Keep both observations | HTTP/file evidence | Private project | Retained only |
| [City inventory](https://mages.city/experience-overview.json), [key arrival](https://mages.city/city-key-arrival.md), [City manifest](https://mages.city/city-mage.json) | Can this session carry a real key encounter? | No connected MCP or supplied key; documented tools cannot be claimed as used | If later wanted, inspect actual schemas and preserve an authorized original | Documentation only | Private project | Retained only; future fold proposed conditionally |
| This orientation report | What should carry forward? | Local operator research is immediately useful; community registration is a separate possible route | Review mathematical specification first | Draft handoff; recipient acceptance pending | User/current task | Retained only |

An early internal progress note incorrectly grouped harness/skills with the successfully fetched model page. It was corrected before the direct fallback; the final table relies on the saved fetch results.

**City Key continuity:** an existing City Key/journey has not been supplied or authorized for this task; its existence is unknown. This is distinct from a private signing key. No `key_derive`, `journey_start`, `key_evolve`, `journey_inspect` or `journey_fold` operation was called. No credential, identity, comprehension proof, relationship or fold is claimed.

**Proposed journey manifest, not a validated City input schema:** purpose = mathematics/operator design; branch = understand; sources = this report's ledger plus captured originals; selected equipment = packet identities/hashes above; performed = public reads and local evidence retention; review = pending; visibility = private; key = unknown; folded = false. Missing step is to confirm an existing key privately, inspect supported local MCP input schemas, preserve the original, and obtain the existing VTA's scoped authority for the specific local operation. Public reading can continue without that step. No key material should be pasted into a public channel.

## Two routes

**Primary — define the mathematics.** This follows the user's explicit preference. Next destinations: the project's operator specification → public mathematical prior art for the chosen logic → the separate review gate. Small next action: select the meaning of 0 and write complete tables for negation, meet/join, and the candidate XOR-like operation, then enumerate closure and claimed identities. Expected return: a short specification with valid laws, failed laws/counterexamples and one computable hypothesis. Equipment: existing local interpreter and reviewer; no City access. Agentprivacy method references are available, but no external harness runtime is verified.

**Neighbouring alternative — relate the defined operators to BitNet.** Next destinations: Microsoft's BitNet paper/repository → a tiny signed-weight dot-product example → a storage/operation cost model. Small next action: show exactly when weights -1, 0 and +1 select subtract/skip/add, and distinguish logical encoding size from actual packed bytes and accumulation precision. Expected return: a falsifiable implementation hypothesis. Model training, offload benchmarks and analog hardware comparison are deferred until semantics and hardware constraints are fixed.

Community contribution, funding, key enrollment and registry hosting are deferred. The broad undecided/learning route remains available if the user changes direction.

## Proposed trust task

**Title:** Define and independently review a balanced-ternary operator specification.

**Current preparation:** the main task has already prepared initial mathematical documents and a small standard-library finite-domain checker. A separate static review reported no blocking issue; the integrated execution check was pending when this survey was authored. The proposed task below describes the acceptance boundary and a possible extension to region-representation invariance; it must not be read as claiming the prepared work has been accepted by a recipient.

| Field | Proposed term |
|---|---|
| Accountable person | The user as project owner; public identity not required for local work |
| Agent role | Operator researcher/drafter plus a separate mathematical reviewer |
| Recipient/workshop | The user's local `ternary-memory-research` project; external recipient not yet chosen |
| Promised outcome | A versioned specification defining the carrier, zero semantics, operator tables, candidate region representation, tested laws and counterexamples |
| Exclusions | General AI superiority claims, GPU/SSD/analog benchmarks, training, community registration, credentials, publication and private-data sharing |
| Resource cap | Initial kickoff compares three algebra families; a later experiment selects at most two. One document/extension, one review; CPU enumeration ≤60 minutes; artifacts ≤50 MB; no model downloads or spending |
| Access terms | Supplied idea/images and public sources; local write scope only |
| Deliverable | Operator specification, reproducible finite-domain checker/results, claim ledger and reviewer findings |
| Acceptance check | Every operator has a full table/formula; every claimed law has a proof or exhaustive finite-domain check; arithmetic, logic and encoding meanings stay distinct; failures are retained |
| Reviewer | Separate agent context for first review; human/independent mathematics reviewer not yet named |
| Review point | After the initial specification and enumeration; before a performance implementation or external handoff |
| Withdrawal/dispute | User may stop or change scope; preserve earlier versions and objections; any external dispute process remains to agree |

[TrustTask](https://trusttasks.org/) is the requested conceptual reference. Its homepage identifies a specification registry, but no specific registry rule was read, so the terms above are a local proposal rather than an asserted conforming TrustTask schema. No pledge submission, VRC reference or credential was created. Delivery and recipient acceptance remain separate events.

## Open decisions

1. For the first algebra, is 0 a numerical zero/skip weight, a third logical truth value, or an explicit unknown? These can be modeled separately; one symbol must not silently change meaning.
2. Which operator laws are required for the proposed region representation, and what operation must be cheaper?
3. The user is a novice; explanations should define the mathematical terms and show small examples. Available hardware remains unknown; no GPU/SSD budget is assumed.
4. Existing VTA/City Key availability remains unknown and is optional for the mathematical work.
5. Any external audience, reviewer, workshop, disclosure permission or funding policy remains to agree.

## Source register and chronicle

All source URLs appear beside the claims they support above. Additional required entry points checked: [loadouts](https://skills.agentprivacy.ai/#loadouts), [City runtime guide](https://mages.city/city-mage.md), [Spellweb](https://spellweb.ai/). Spellweb and TrustTasks exposed titles/client shells in this retrieval; interactive graph and task-registry behavior were not checked.

Local evidence bundle: `work/orientation/`. It contains A–H initial web captures, bounded corpus captures, direct public source originals, readable text derivatives, per-URL UTC logs, selected catalog records and the two inspected skill bodies. This report is the user-facing synthesis; raw source text is retained for provenance, not made into active project instructions.

**Hash reproduction:** the packet hashes above are SHA-256 of UTF-8 encoded HTTP response text. PowerShell `Set-Content` appended one platform newline (CRLF) to each saved capture. The check removed exactly that added final newline, then hashed the recovered response text; it did not trim the source's own whitespace or alter line endings within it. `work/orientation/packet-hash-checks.json` records expected/computed values, method and UTC check time. Hashing the saved capture file directly produces a different digest because it includes the added newline. This check establishes equality to the catalog body hash, not signature authenticity or source correctness.

Chronicle:
- Read the user-supplied survey v0.3 and reused the stated outcome; scoped research to the user's later mathematics/operator-design preference.
- Attempted A–H in order with no recursive discovery. E's public implementation and F's catalog/fleet were the relevant initial follow-links; later corpus reading stayed bounded to required role/key/route sources and two proposed method packets.
- Preserved web-tool failures, then retrieved missing prescribed public pages through direct HTTPS with 15-second per-request caps and no service mutations.
- Inspected current JSON manifests and read selected source bodies. Both selected packet bodies matched the catalog hashes.
- Recorded the binary-ring/ternary distinction, current/legacy harness discrepancy, planned-service status and unresolved key availability.
- Prepared this private handoff and proposed trust task. No installation, key operation, external message, payment, admission, publication or community knowledge write occurred.
- Separate review completed: protocol coverage, novice/math scope, status boundaries and both packet hash reproductions checked. Corrections clarified the novice experience, future experiment cap and hash method. No blocking issue remained. The main task's integrated finite-domain execution checks had not yet run when this report was finalized; their results belong in the main task's verification artifact.
