# Codex project setup evidence

Verified on 2026-09-11 using the protocol generated by the installed Codex CLI.

## Result

The existing folder `C:\Users\atooz\Programming\ternary-memory-research` is registered as the Codex project `ternary-memory-research`. The current chat is associated with that project.

| Field | Verified value |
|---|---|
| Project ID | `01a09305-af97-7920-8927-4b8c32e98c57` |
| Project name | `ternary-memory-research` |
| Project root | `C:\Users\atooz\Programming\ternary-memory-research` |
| Current task ID | `01a092e2-9604-7b42-a684-4917b188dee6` |
| Task project ID | `01a09305-af97-7920-8927-4b8c32e98c57` |
| Task working directory at verification | `C:\Users\atooz\Documents\Codex\2026-09-11\cre` |

## Supported operation and verification

1. The installed CLI generated its experimental JSON schema with `codex app-server generate-json-schema --experimental --out <workspace-directory>`.
2. The schema defines `project/import` with `name`, `roots`, `idempotencyKey`, and optional `threads`. It also defines `project/read`, `project/list`, and `thread/read`.
3. A short-lived local stdio app-server received an initialization request and a `project/import` request naming the folder and existing current task. Filesystem escalation was approved for access to the app's storage.
4. `project/read` returned the project with the exact intended name and root. `thread/read` returned the current task with that project's ID.
5. A fresh short-lived server connection then confirmed persistence: `project/list` returned 23 projects with one exact name match; `thread/read` again returned the same project ID for the current task.
6. All helper server processes exited. No task was created, resumed, interrupted, or sent a new turn by this setup procedure. No app-state JSON or SQLite files were edited directly.

The project association is verified in Codex's persisted metadata. Live sidebar rendering was not inspected because this session exposes no native UI automation. The current task's working directory remains the original generated directory; project association and a running task's working directory are separate settings.

## Final working-directory attempt

After the project files were copied and verified, one bounded `thread/settings/update` request supplied the existing task ID and `cwd = C:\Users\atooz\Programming\ternary-memory-research`. The short-lived server could read the task's persisted metadata, but its settings-update method returned error `-32600`, `thread not found: 01a092e2-9604-7b42-a684-4917b188dee6`. That channel therefore could not update this active task's runtime settings. No resume, load, interruption, new turn, or retry was attempted. The helper process exited.

The verified final result is a registered project and associated existing chat, with research files in the intended Programming folder. The active task's working directory was not changed. Future work should explicitly use the project folder as its working directory until the app changes the task's runtime workspace through its own supported controls.

## Earlier CLI launcher result

`codex app C:\Users\atooz\Programming\ternary-memory-research` was tried first because its help documents opening a workspace. It incorrectly failed to detect the running desktop installation, attempted to open the Windows installer, and received `Access is denied`. Despite exit code 0, this was treated as a failure. No installation was performed or retried. The successful operation above used the installed CLI's project protocol instead.
