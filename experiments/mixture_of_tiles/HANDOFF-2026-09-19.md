# Handoff: mixture-of-tiles / disk-resident CPU inference (2026-09-19)

Paste the block below as the opening prompt for the agent taking over this lane.

---

You are taking over the **mixture-of-tiles** lane of the repo at `C:\Users\atooz\Programming\ternary-memory-research`
(Windows 11, Git Bash + PowerShell, Python 3.12, torch 2.5.1+cu121, numpy, tokenizers; public at
github.com/JadeZaher/ternary-memory-research). Your folder is `experiments/mixture_of_tiles/`. You may read
anything in the repo; modify only that folder, `research/disk-inference-tier.md`, and new ledgers under
`outputs/mixture-of-tiles-*.json`. Do not touch `experiments/general_model/`, `experiments/unified_scaling/`,
`data/`, or anything on the GPU.

**Hard machine rules.** This lane is CPU-only: set `os.environ["CUDA_VISIBLE_DEVICES"] = ""` at the top of every
script and test, use `torch.device("cpu")`, `torch.set_num_threads(4)` at most. A GPU training job may be running
at any time (check `nvidia-smi`); never start CPU benchmarks while GPU memory used is above ~7 GB (a spilling
trainer saturates system RAM bandwidth and froze the machine on 2026-09-19). Keep benchmark prompts short by
default (32 prompt tokens, 16 generated) and print per-token timings as you go. Reap every process you start.

**What exists and is verified** (read `experiments/mixture_of_tiles/AGENTS.md` first, then the code):
- `tile_store.py`: base-3 packing at 1.6 bits/weight (exact), `TileStore.build/open/read_tile`, `TileCache` (LRU,
  hit/miss/bytes/latency, optional prefetch, optional injected miss latency), `install_tile/uninstall_tile`
  (reloaded tiles give logits identical to the original model, delta 0.0).
- `cpu_decoder.py`: `StreamingDecoder`, batch-1 greedy decode with full recompute per token; a tile can only run
  after it came through the cache and an evicted tile raises `TileNotResidentError`.
- `bench_cpu_decode.py`, `sweep_tile_count.py`, `test_mixture_of_tiles.py` (5 tests, all pass on CPU).
- Ledgers: `outputs/mixture-of-tiles-bench.json`, `outputs/mixture-of-tiles-sweep-plan.json`.
- The model under test is `experiments/general_model/general_model.py::GeneralRoutedLM` (hybrid Mamba/attention
  tiles, free per-token routing, value-based exit). A trained checkpoint `outputs/checkpoints/general-G1-full-best.pt`
  appears when the night-1 run finishes (~2026-09-20 morning); until then use random init and say so.

**Measured so far (random-init p512, fp32-unpacked tiles, 4 threads):** ~1 token/s; pageable tile library
6.85 MB, resident part 19.2 MB (embeddings dominate); capacity 2 -> 4 tiles cuts bytes/token 145x; a random
router touches all four tiles per token so the working set equals the library; fixed-bytes tile-count sweep is
infeasible past 8 tiles at width 512 because the mixer does not shrink with the FFN.

**Your ladder** (from `research/disk-inference-tier.md`, section 5; each rung = one exactness test + one benchmark
row appended to the ledger; do them in order and stop to report after each):
1. **State caching**: per-tile Mamba state and attention KV carried across generated tokens so each new token costs
   one pass over its own hops. Test: generated ids and per-token logits identical to full recompute (1e-4).
   Report tokens/s before/after at 32/64/128-token prompts.
2. **Ternary lookup-table matmul on CPU**: consume the packed 2-bit tiles directly (no fp32 unpack); reference is
   `experiments/bitlinear.py` (per-256-block absmean scales, additive GEMM). Test: exact against the fp32 path.
   Report tokens/s and bytes/token.
3. **Trained-router working set**: rerun `bench_cpu_decode.py` on `general-G1-full-best.pt`; report tiles/token,
   working set per sequence, hit rate vs capacity, tile-usage entropy. This is the number that decides whether
   paging from disk can ever pay.
4. **Shared-mixer tiled-FFN design** (only after 3): propose, in a short design note in the folder, a tile whose
   mixer is shared and only the FFN experts are tiled, so 64 tiles fit the same bytes; do not modify
   `general_model.py` yourself, write the note and the fixed-bytes plan.
5. **Cold-disk measurement**: replace injected latency with unbuffered reads (`FILE_FLAG_NO_BUFFERING` via ctypes)
   for the miss path; report real miss latency for 1.9 MB tiles.

**Conventions.** Full-word identifiers, one-line "what" comments in code, the "why" goes in `AGENTS.md`. Apply all
fixes first and run the test file once at the end (no test-fix loops). Every number you report must come from a
ledger or a test line you can quote. Report format: files touched, verbatim test lines, a benchmark table,
deviations from this brief with reasons, under 700 words.
