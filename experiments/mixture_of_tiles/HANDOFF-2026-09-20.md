# Handoff: mixture-of-tiles / disk-resident CPU inference (written 2026-09-19 23:30 MDT, for the next session)

Paste the block below as the opening prompt for the agent taking over this lane. It supersedes
`HANDOFF-2026-09-19.md` (same lane, one rung further); read that file only for history.

## Takeover update, 2026-09-20 UTC

The adversarial review below is now complete. Runner safety/accounting issues and the unsupported
`collect_received` omission were fixed. One integrated CPU run passed all ten tests, including dense-GEMM
cached/full parity on tiny models at 32/64/128-token prompts. The exact evidence and source hashes are in
`outputs/mixture-of-tiles-rung1-validation-2026-09-20.json`; the lane `AGENTS.md` has the result table.
GPU/RAM snapshots are in `outputs/mixture-of-tiles-monitor-2026-09-20.json`. They showed the same trainer
still active at the latest logged step 750. This is a sampled observation, not a continuously running watcher.

The user directed work toward small CPU feasibility experiments alongside training rather than waiting
idle for the trainer. Tiny correctness tests have run; the full-size benchmark remains unmeasured. The
runner now rejects unavailable telemetry, has recurring token-boundary checks, refuses existing output
paths, and defaults to the new state-cache ledger. `--ignore-guards` has been removed; thresholds may only
tighten the hard limits. `--check-only` checks readiness without allocating a model. Timing includes
prefill/decode forwards plus unslept injected latency, not monitoring or token-selection overhead.

Use the older snapshot below for historical test lines and the trainer command. Do not repeat the review
or tests solely because that snapshot calls them outstanding. Next decisive evidence is the matched p512
full/cached benchmark when machine conditions and trainer scheduling permit it. Do not infer p512 speed,
trained quality or a bounded-RAM deployment from the tiny-model exactness results.

Final readiness check in this takeover exited 2 before model allocation: GPU usage was 5348 MiB,
but free RAM was 3.8566 GiB, below the 4 GiB minimum. The verbatim check is captured in the monitor ledger.
No benchmark was started and no persistent watcher remains. The independent final review approved the
correctness/safety changes and verified all six source hashes against the compact validation ledger.

---

You are taking over the **mixture-of-tiles** lane of the repo at `C:\Users\atooz\Programming\ternary-memory-research`
(Windows 11, Git Bash + PowerShell, Python 3.12, torch 2.5.1+cu121, numpy, tokenizers, psutil 7.2; public at
github.com/JadeZaher/ternary-memory-research). Your folder is `experiments/mixture_of_tiles/`. You may read
anything in the repo; modify only that folder, `research/disk-inference-tier.md`, and new ledgers under
`outputs/mixture-of-tiles-*.json`. Do not touch `experiments/general_model/`, `experiments/unified_scaling/`,
`experiments/mamba/`, `data/`, or anything on the GPU.

## The GPU job on the other architecture — check it before every heavy step

A trainer for the **general-model track** (the hybrid ternary Mamba routed LM, a different architecture lane
from yours) is running on the only GPU:

```
python experiments/general_model/train_general.py --data-dir data/general --preset p512 --tile-kinds mamba,mamba,mamba,attn
  --routing free --exit-mode value --exit-lambda 0.02 --explore 0.3 --min-hops 2 --max-hops 8 --exit-warmup 500
  --router-cond state --adapter-cond hop --seq-len 1024 --batch-size 2 --grad-accum 8 --grad-checkpoint
  --max-steps 2523 --eval-interval 250 --save-interval 250 --target-bpb 1.3909 --vram-fraction 0.85 --tag G1-full --resume
```

* As of 2026-09-19 23:09 it was PID 24816 at **step 500 of 2523**, 10,145 s elapsed for those 500 steps
  (`outputs/general-G1-full-log.json` → `history[-1]`), so it should finish around **2026-09-20 midday**.
  GPU memory in use swung between 5.8 and 7.3 GB of 8.2 GB during this session; free RAM was 1.9 GB of 32 GB
  and the machine held ~657 processes. It froze once on 2026-09-19 when a CPU benchmark ran next to it.
* **Rule:** no CPU benchmark while `nvidia-smi` shows more than ~7 GB used or free RAM is under 4 GB. The
  runner now enforces this itself (`bench_cpu_decode.py` refuses to start; no guard bypass is available).
  Tests on the tiny models are fine at any time.
* How to know it is done: `Get-CimInstance Win32_Process -Filter "Name='python.exe'"` no longer lists
  `train_general.py`; `outputs/checkpoints/general-G1-full-best.pt` gets a new mtime (it is 19:04 on
  09-19 now, i.e. an early-step checkpoint that already loads); the log's last `history` row reaches step 2523
  or `target_bpb_pass` flips. Never kill, pause or `--resume` that job yourself.
* Do not start your own long background processes; reap everything you start before you stop.

## State of this lane (verified 2026-09-19 23:27)

Rung 1 of the ladder — **state caching** — is implemented and passes its exactness test; its **benchmark
row is not yet measured** (blocked by the rule above). Uncommitted working tree; commit it first:

```
git add experiments/mixture_of_tiles research/disk-inference-tier.md
git commit -m "mixture-of-tiles rung 1: (hop, tile) state-cached decoder, exactness tests, guarded benchmark runner"
```

Files (all in `experiments/mixture_of_tiles/`; read `AGENTS.md` "State caching" first, then the code):
* `state_cache.py` (new): `StreamStateCache` keyed by **(hop, tile)** — every hop gathers its own causal
  subset, so one state per tile would be wrong; `mamba_forward_with_state` (chunked conv + selective scan
  continuing from a carried state, the block's own `_selective_scan` for the zero-state part),
  `attention_forward_with_state` (cached K/V, mask `key <= past + query`), `hybrid_tile_forward_with_state`
  (re-checks residency and raises `TileNotResidentError`, since the module pre-hook does not fire on the
  piecewise path).
* `cached_decoder.py` (new): `CachedStreamingDecoder(StreamingDecoder)` — one prefill chunk then one
  1-token chunk per generated token, the eval-mode hop loop of `GeneralRoutedLM.forward` re-run lane-locally,
  same paging funnel (`_page_in`). Refuses `exit_policy` `capacity`/`random` and `force_random_tiles`
  (no causal per-token answer). Summary adds `prefill_ms`, `decode_tokens_per_second`,
  `decode_mean_mb_per_token`, `state_bytes`, `stream_lengths`, `tile_token_histogram`.
* `cpu_decoder.py`: `_page_in` factored out of `_run_tile`; behaviour unchanged (reference decoder).
* `bench_cpu_decode.py` (rewritten): `--mode full|cached|both`, `--prompt-tokens 32,64,128`, defaults 32
  prompt / 16 new, per-token live lines, GPU/RAM/RSS guards, rows carry `mode` and `prompt_tokens`.
* `test_mixture_of_tiles.py`: tests e (`test_state_chunking`) and f (`test_cached_decode_exactness`) added.

Verbatim test lines (CPU, 4 threads, `python experiments/mixture_of_tiles/test_mixture_of_tiles.py`):
```
  tile 0 (mamba): chunks [(0, 5), (5, 6), (6, 12)] max |delta| 1.863e-08 stream tokens 12
  tile 2 (attn ): chunks [(0, 5), (5, 6), (6, 12)] max |delta| 2.235e-08 stream tokens 12
  free routing, allow_exit=False, capacity 1: ids [39, 264, 264, 264, 264, 264] prefill max |delta| 2.682e-07 step max |delta| 2.086e-07 mean hops ref 4.00 cached 4.00 misses 27 evictions 26 streams 7 state 38,400 B
  free routing, allow_exit=True, capacity 1: ids [39, 39, 39, 351, 505, 505] prefill max |delta| 3.576e-07 step max |delta| 1.788e-07 mean hops ref 2.59 cached 2.40 misses 13 evictions 12 streams 7 state 32,256 B
  path-conditioned routing, capacity 2: ids [148, 215, 215, 215] prefill max |delta| 1.825e-07 step max |delta| 2.161e-07 mean hops ref 4.00 cached 4.00 misses 14 evictions 12 streams 11 state 47,104 B
  exit_policy=capacity refused: exit_policy='capacity': 'capacity' ranks a token against the...
ALL MIXTURE-OF-TILES TESTS PASSED
```
Read these as: identical greedy ids, logits within ~3e-7 of full recompute (tolerance 1e-4), with a real mix
of exits (mean hops 2.59 between min 2 and max 4), and stream state surviving 26 weight evictions. The
`mean hops ref 2.59 / cached 2.40` gap is two populations (reference: whole final prefix; cached: decode
tokens only), not a discrepancy.

## Your ladder (unchanged from `research/disk-inference-tier.md` section 5; stop and report after each rung)

0. **Finish rung 1's evidence** as soon as the trainer is gone:
   `python experiments/mixture_of_tiles/bench_cpu_decode.py --mode both --prompt-tokens 32,64,128 --new-tokens 16 --capacity 2,4 --no-cold --output outputs/mixture-of-tiles-state-cache.json`
   (random init, say so; full-recompute rows at capacity 2 / 128 tokens take ~1 min each — use `--mode cached`
   alone if time is short). Then add the measured table to `AGENTS.md` under "Measured" and a rung-1 status
   line in `research/disk-inference-tier.md`; quote `tokens_per_second` (all-in) and
   `decode_tokens_per_second` separately. Do **not** write to the default output path — it would overwrite
   the committed rung-0 ledger `outputs/mixture-of-tiles-bench.json`.
   Also outstanding: an adversarial review of the rung-1 code was started and stopped before it reported;
   run `/code-review high` on the five files above before building on them. Its last line pointed at
   activation quantisation / norm granularity — verified by inspection: `quantize_act=False` in both
   `MambaConfig` and the attention `BitLinear`s, and `BitRouteRMSNorm` is per token, so nothing in a tile is
   per-sequence except the mixer.
1. (done, see above)
2. **Ternary lookup-table matmul on CPU**: consume the packed tiles directly. The store is **base-3
   (5 trits/byte), not 2-bit** — decide first whether the LUT path unpacks base-3 or the store gains a 2-bit
   variant; reference is `experiments/bitlinear.py` (per-256-block absmean scales, additive GEMM). Test: exact
   against the fp32 path. Report tokens/s and bytes/token.
3. **Trained-router working set** on `general-G1-full-best.pt` once the run ends: tiles/token, working set,
   hit rate vs capacity, and quote `tile_token_entropy_bits` (per-token assignments), not the call histogram.
4. **Shared-mixer tiled-FFN design note** (after 3), no edits to `general_model.py`.
5. **Cold-disk measurement** with `FILE_FLAG_NO_BUFFERING` via ctypes replacing injected latency.

## Gotchas
* `experiments/mixture_of_tiles/work/` holds another agent's web-research capture (flash-resident inference
  papers); it is not code, do not delete or cite it as a measurement.
* `uninstall_tile` keeps the fp32 master parameters in RAM (only the eval cache and residency flag go).
  The byte table does not include the full master/derived/temporary allocation footprint; a bounded-RAM
  deployment remains unproven and needs allocation measurements as well as artifact accounting.
* Windows page cache makes the first row of a run the only cold one; compare capacities within a run.
* Conventions: full-word identifiers, one-line "what" comments, the "why" in `AGENTS.md`, all fixes first then
  one test run, every number quoted from a ledger or a test line. Report: files touched, verbatim test lines,
  benchmark table, deviations with reasons, under 700 words.
